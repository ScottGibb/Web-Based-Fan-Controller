[project]
name = "web-based-fan-controller-backend"
version = "0.1.0"
description = "Flask backend for the web-based fan controller"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1",
    "flask-cors>=6.0",
]

[tool.uv]
package = false
